# Pygame 3D Renderer

A small 3D renderer project using Pygame as the base library for drawing 3D graphics.

## Setup

```bash
python -m venv .venv
source .venv/bin/activate
python -m pip install -e .
```

The project currently contains structure only; rendering logic will be added later.
